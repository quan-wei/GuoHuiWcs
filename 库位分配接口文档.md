# 库位分配接口文档

## 基础信息

- Base URL: http://{host}/api/location
- Content-Type: pplication/json

---

## 1. 自动分配库位

系统自动查找空闲库位并分配。

**POST** /api/location/allocate

### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| MaterNo | string[] | 是 | 物料条码列表，用于 WMS 校验并获取重量 |
| StartPoint | string | 是 | 起点库位编码（Reserve5） |
| AllowUpperLevels | ool | 否 | 是否允许分配到上层货架，默认 	rue |

`json
{
    "MaterNo": ["BARCODE001", "BARCODE002"],
    "StartPoint": "A01",
    "AllowUpperLevels": true
}
`

### 成功响应 (200)

`json
{
    "Success": true,
    "LocationCode": "A01-01-01",
    "LocationType": "一层货架",
    "WeightKg": 150.5,
    "PallNo": "PALL202608090001",
    "Message": "分配成功"
}
`

| 字段 | 类型 | 说明 |
|------|------|------|
| Success | ool | 是否成功 |
| LocationCode | string | 分配的库位编码 |
| LocationType | string | 库位类型（一层货架/二层货架/三层货架/四层货架/地面库位） |
| WeightKg | decimal | 物料总重量 (kg) |
| PallNo | string | 生成的托盘号 |
| Message | string | 结果描述 |

### 失败响应 (400)

`json
{
    "Success": false,
    "Message": "无可用库位：一层已满，且上层库位均不满足重量限制"
}
`

### 业务逻辑

1. 校验 StartPoint 不能为空
2. 调用 WMS 同步物料条码，获取重量
3. 优先分配一层货架，若一层已满且 AllowUpperLevels=true，则依次尝试二、三、四层
4. 上层货架需满足货架对重量限制（2500kg）
5. 分配成功后创建 PallMater 记录，并调度 AGV 搬运任务
6. AGV 任务创建成功后，**自动释放起点库位**

---

## 2. 分配到指定库位（合并接口）

将物料分配到指定的终点库位。根据终点编码前缀自动判断是入库分配还是出库操作。

**POST** /api/location/allocate/{locationCode}

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| locationCode | string | 终点库位编码（Reserve5） |

### 请求体

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| MaterNo | string[] | 条件必填 | 物料条码列表。终点非 G 开头时必填，G 开头时可为空 |
| StartPoint | string | 是 | 起点库位编码（Reserve5） |

### 入库分配（终点非 G 开头）

`json
{
    "MaterNo": ["BARCODE001"],
    "StartPoint": "A01"
}
`

### 出库操作（终点以 G 开头）

`json
{
    "StartPoint": "B02"
}
`

### 成功响应 (200)

`json
{
    "Success": true,
    "LocationCode": "A01-02-03",
    "LocationType": "二层货架",
    "WeightKg": 200.0,
    "PallNo": "PALL202608090002",
    "Message": "分配成功"
}
`

### 失败响应 (400)

`json
{
    "Success": false,
    "Message": "物料号不能为空"
}
`

### 业务逻辑

| 终点前缀 | 行为 | MaterNo | 物料校验 | PallMater | 重量 |
|----------|------|---------|----------|-----------|------|
| 非 G 开头 | 入库分配 | 必填 | WMS 校验 | 创建 | 实际重量 |
| G 开头 | 出库操作 | 可为空 | 不校验 | 不创建 | 0 |

**入库分配流程：**
1. 校验终点库位存在、空闲、未禁用
2. 校验 MaterNo 不为空
3. 调用 WMS 同步物料条码，获取重量
4. 创建 PallMater 记录
5. 检查货架对重量限制
6. 占用终点库位，调度 AGV 搬运任务
7. AGV 成功后，**自动释放起点库位**

**出库操作流程：**
1. 校验终点库位存在、空闲、未禁用
2. 跳过物料校验，重量为 0
3. 占用终点库位，调度 AGV 搬运任务
4. AGV 成功后，**自动释放起点库位**

---

## 3. 释放库位

手动释放指定库位，将其恢复为空闲状态。

**POST** /api/location/release/{locationCode}

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| locationCode | string | 库位编码（LocationCode） |

### 成功响应 (200)

`json
{
    "Success": true,
    "Message": "库位已释放"
}
`

### 失败响应 (400)

`json
{
    "Success": false,
    "Message": "库位释放失败，可能已是空闲状态"
}
`

---

## 4. 查询货架对负载

查询指定货架所在货架对的当前负载情况。

**GET** /api/location/group-load/{shelfCode}

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| shelfCode | string | 货架编码 |

### 成功响应 (200)

`json
{
    "GroupShelfs": ["1", "2"],
    "CurrentWeightKg": 1800.0,
    "LimitWeightKg": 2500.0,
    "RemainingWeightKg": 700.0,
    "TierLoads": {
        "二层货架": 800.0,
        "三层货架": 600.0,
        "四层货架": 400.0
    }
}
`

| 字段 | 类型 | 说明 |
|------|------|------|
| GroupShelfs | string[] | 货架对包含的货架编码列表 |
| CurrentWeightKg | decimal | 当前总重量 (kg) |
| LimitWeightKg | decimal | 重量限制，固定 2500kg |
| RemainingWeightKg | decimal | 剩余可用重量 (kg) |
| TierLoads | object | 各层货架的当前重量明细 |

---

## 公共错误码

| HTTP 状态码 | 场景 |
|-------------|------|
| 200 | 操作成功 |
| 400 | 业务校验失败，详见 Message 字段 |

## 常见失败原因

| Message | 说明 |
|---------|------|
| 起点不能为空 | StartPoint 未传入 |
| 物料号不能为空 | 入库分配时 MaterNo 为空 |
| 终点库位不存在 | locationCode 对应的库位不存在 |
| 终点库位已被占用 | 目标库位已有货物 |
| 终点库位已禁用 | 目标库位 EnableFlag 为 false |
| WMS 未返回任何物料重量，请检查物料码 | 所有物料码在 WMS 中均未匹配到重量 |
| 所在货架对已超重限制 | 分配后会导致货架对总重量超过 2500kg |
| 无可用库位：一层已满，且上层库位均不满足重量限制 | 自动分配时无符合条件的库位 |
| AGV 搬运任务创建失败 | AGV 系统返回异常，已自动回滚库位分配 |